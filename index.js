/**
 * @format
 */

import { AppRegistry } from 'react-native';
import Block from '@/components/Block';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Block);
